/**
 * Barrel export for all custom fixtures
 * Tests import from this single entry point:
 *   import { test, expect } from '../../src/fixtures';
 */

export { test, expect } from './salesforce-page.fixture.js';
